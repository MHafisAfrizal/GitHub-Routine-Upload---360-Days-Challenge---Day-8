// Day 8 script (optional behavior)
document.querySelectorAll('.card button').forEach(button => {
  button.addEventListener('click', () => {
    alert('Terima kasih telah memilih paket kami!');
  });
});
